"""Passos individuais do pipeline de extração (s1a … s5).

O prefixo numérico reflete a ordem de execução (ver
skeltrans.extraction.config.STEPS). Cada módulo expõe `main()` e continua
executável tanto pelos shims na raiz quanto por `python -m`.
"""
